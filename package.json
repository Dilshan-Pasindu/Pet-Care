{
  "name": "petcare",
  "version": "1.0.0",
  "description": "PetCare — Pet Healthcare & Services Management Mobile Application",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npx expo start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test",
    "build:backend": "npm run build --prefix backend",
    "test:backend": "npm test --prefix backend",
    "type-check:frontend": "npm run type-check --prefix frontend",
    "ci": "npm run build:backend && npm run test:backend && npm run type-check:frontend"
  },
  "keywords": [
    "petcare",
    "react-native",
    "expo",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "PetCare Development Team",
  "license": "MIT"
}
